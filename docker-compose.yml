version: '3.8'

services:
  teslamateapi_tgbot:
    image: ghcr.io/sidecloudgroup/teslamateapi_tgbot:latest
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./data/car_state.json:/app/car_state.json
